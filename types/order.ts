// Define the possible types for an order
export type OrderType = 'pickup' | 'delivery' | 'dine-in';

// You might want to add other order-related types here in the future
// export interface Order {
//   id: string;
//   // ... other order properties
// } 